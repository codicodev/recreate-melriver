import React from "react";
import PropTypes from "prop-types";

const Logo = (props) => {
  return (
    <div className="logo">
      <a href="/">CODICO.</a>
    </div>
  );
};

Logo.propTypes = {};

export default Logo;
